<div class="card">
  <div class="card-content">
    <div class="row">
      <% if (req.user) {%>
        <p> En route Capitaine <%= req.user.username %> !!<br>
          Mettons les voiles sur la mer de Docker.
        </p>
      <% } else { %>
        <p> Not logged in </p>
      <% } %>
    </div>
  </div>
</div>
